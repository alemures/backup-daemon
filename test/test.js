const { describe } = require('mocha');
const { it } = require('mocha');
const { assert } = require('chai');

describe('test', () => {
  it('should be true', () => {
    assert(true, 'true');
  });
});
